export const GitHubBadge = ({ title, background, color, borderColor="transparent" }) => {
  return (
      <span 
          style={{ 
              backgroundColor: background, 
              color: color, 
              borderColor: borderColor 
          }}
          className={`inline-flex px-2 text-sm leading-0 font-semibold border border-solid rounded-full whitespace-nowrap `}
      >{title}</span>
  )
}
