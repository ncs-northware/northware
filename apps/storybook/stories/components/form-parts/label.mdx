import * as LabelStories from "./label.stories"
import { Meta, Canvas, Controls, Stories  } from "@storybook/blocks"
import { ViewSource, ShadcnDocs } from "../../../components/storybook-buttons"

<Meta of={LabelStories} />

# Label

Zeigt ein barrierefreies Label, das mit Formular-Elementen zusammenhängt.

<ShadcnDocs path="components/label" />
<ViewSource path="components/form-parts/label.tsx" />

<Canvas of={LabelStories.Default} />
<Controls of={LabelStories.Default} />

## Benutzung

```tsx
import { Label } from "@northware/ui/components/form-parts/label"

<Label htmlFor="email">Your email address</Label>
```