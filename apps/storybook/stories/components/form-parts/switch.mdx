import * as SwitchStories from "./switch.stories"
import { Meta, Canvas, Controls, Stories  } from "@storybook/addon-docs/blocks"
import { ViewSource, ShadcnDocs } from "../../../components/storybook-buttons"

<Meta of={SwitchStories} />

# Switch

Ein Formularfeld, mit dem der User einen Eintrag an- und ausschalten kann.

<ShadcnDocs path="components/switch" />
<ViewSource path="components/form-parts/switch.tsx" />

<Canvas of={SwitchStories.Default} />
<Controls of={SwitchStories.Default} />

## Benutzung

```tsx
import { Switch } from "@northware/ui/components/switch"

<Switch />
```

<Stories />