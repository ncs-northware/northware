import { Meta, Canvas, Controls, Stories } from "@storybook/blocks"
import * as AccordionStories from "./accordion.stories"
import { ViewSource, ShadcnDocs } from "../../../components/storybook-buttons"

<Meta of={AccordionStories} />

# Accordion

Einige vertikal gestapelte interaktive Überschriften, die jeweils einen Inhaltsabschnitt enthalten.

<ShadcnDocs path="components/accordion" />
<ViewSource path="components/panels/accordion.tsx" />

<Canvas of={AccordionStories.Default} />
<Controls of={AccordionStories.Default} />

## Benutzung

```tsx
import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from "@northware/ui/components/panels/accordion"

<Accordion type="single" collapsible>
  <AccordionItem value="item-1">
    <AccordionTrigger>Is it accessible?</AccordionTrigger>
    <AccordionContent>
      Yes. It adheres to the WAI-ARIA design pattern.
    </AccordionContent>
  </AccordionItem>
</Accordion>
```