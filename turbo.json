{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "globalEnv": ["DATABASE_URL", "CLERK_SECRET_KEY"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [
        ".next/**",
        "!.next/cache/**",
        "dist/**",
        "build/**",
        "storybook-static"
      ]
    },
    "dev": {
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "cache": false,
      "persistent": true
    },
    "check": {
      "dependsOn": ["^check"],
      "cache": false
    },
    "//#check:root": {},
    "fix": {
      "dependsOn": ["^fix"],
      "cache": false
    },
    "//#fix:root": {},
    "tsc-check": {
      "dependsOn": ["^tsc-check"]
    }
  }
}
